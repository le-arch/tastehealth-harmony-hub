
# This configuration file enables the Edge Function to run on a schedule
# The cron expression "0 8 * * *" means "run every day at 8:00 AM UTC"

[functions.auto-generate-notification]
schedule = "0 8 * * *"
verify_jwt = false
